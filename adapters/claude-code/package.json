{
  "name": "@airuntimeguard/adapter-claude-code",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": { "guard-claude-code": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test 'dist/**/*.test.js'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": { "@airuntimeguard/adapter-shared": "workspace:*" },
  "devDependencies": { "@types/node": "^22.10.0", "typescript": "^5.7.0" }
}
